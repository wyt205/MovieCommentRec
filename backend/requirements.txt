# ---- Web 框架 ----
fastapi>=0.110
uvicorn[standard]>=0.27
# ---- ORM / 数据库 ----
sqlalchemy>=2.0
pymysql>=1.1
# ---- 配置 / 校验 ----
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
# ---- 爬虫（TMDb 爬虫使用 requests）----
requests>=2.31
PySocks>=1.7.1  # 支持 SOCKS5 代理（Clash/V2Ray 本地常用 socks5://127.0.0.1:7891）
# ---- HTTP 客户端（调用云端大模型 API / 下载海报 时需要）----
httpx>=0.27
# ---- 数值计算（RAG 余弦相似度检索）----
numpy>=1.24
# ---- Agent（LangChain + 大模型 function calling）----
# 模型待定时可不装，后端其它功能不受影响；启用 Agent 前 pip install 即可
langchain>=0.3
langchain-openai>=0.2
langchain-community>=0.3
